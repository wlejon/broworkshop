{
    "name": "maker-agent-bundler",
    "private": true,
    "type": "module",
    "description": "Author-time only: esbuild the earendil-works/pi agent harness + maker-agent seams (OpenRouter + brolm providers, maker tools, look) into a single self-contained ESM file (../maker.bundle.js) that bro's QuickJS loads. Not shipped to the runtime.",
    "scripts": {
        "build": "node build.mjs"
    },
    "dependencies": {
        "typebox": "1.1.38",
        "partial-json": "0.1.7",
        "@opentelemetry/api": "1.9.0",
        "ignore": "7.0.5",
        "yaml": "2.9.0"
    },
    "devDependencies": {
        "esbuild": "^0.24.0"
    }
}
